# Diagnostic Record: NB11 E2/h=3 Kaggle Re-run

## Scope and Integrity Boundary

This is an audit-only diagnostic submission for the paper-recertification E2/h=3 alignment blocker. It must not overwrite, alter, discard, or validate the pre-existing residual artifact at `docs/resultados/residuos-multihorizon/11-lstm/h3/lstm_residuals_h3.csv`. A remote submission is not a validated paper result. Phase 5's local alignment gate remains unchanged and must not be run against downloaded diagnostics unless the required provenance and immutable residual keys are present.

## Preflight Provenance

| Field | Value |
|---|---|
| Diagnostic UTC | `2026-07-12T15:27:02Z` |
| Local kernel directory | `notebooks/11_lstm_multihorizon/h3` |
| Local kernel identifier | `alexhuaracha/11-lstm-multihorizon-h3` |
| Local notebook | `11_lstm_h3.ipynb` |
| Notebook SHA-256 | `04a59cab99b6949fa1601ea3880a05d7374ac5e9ee23e52c912d2b59186fe0d5` |
| Metadata SHA-256 | `a0e61322065db7c1015f2df006a21283d1289f1e0e7333bdebabb77a3f7f0663` |
| Git HEAD | `330c9b70ba4803ee3d4f4926839d28972763c68f` |
| Kaggle CLI | `2.1.2` via `uv run kaggle` |
| Authentication preflight | `ACCESS_TOKEN` configuration for username `alexhuaracha`; remote status query succeeded |
| Remote status before submission | `KernelWorkerStatus.COMPLETE` |
| Local E2 input | `data/processed/headways_E2.parquet` |
| Local E2 input SHA-256 | `82a34eaffc79cd82346d4595a2e72f5d3ffb751ed37fa0fc0cde3a8f8fb345d4` |
| Local E2 input rows | `1,590,659` |
| Local E2 split counts | train `1,127,043`; val `240,960`; test `222,656` |
| Current E2 train-p99 ceiling | `28.4679230336` minutes |

## Input References

The inspected local `kernel-metadata.json` declares no `dataset_sources`. It declares these unversioned upstream kernel sources:

- `alexhuaracha/04-preprocessing`
- `alexhuaracha/10-baselines-multi-horizonte`

No immutable Kaggle dataset or kernel version reference is present in the local metadata. This is a provenance gap until remote execution metadata/output establishes versioned inputs.

## Notebook Contract Observed

The generated notebook computes the p99 ceiling from non-null train rows, applies that ceiling to all train/validation/test rows, and writes the six-column residual export `lstm_residuals_h3.csv`. The current residual export contains no immutable window keys. A successful run can therefore be an execution diagnostic only; it cannot validate the local Phase 5 alignment or paper recertification without a keyed export and matching provenance.

## Working Tree Preservation

The working tree was already dirty with the authorized Phase 3/4/6 notebook, builder, test, task, exploration, and `.codegraph/` changes. This diagnostic record is additive. No pre-existing file is overwritten.

## Submission and Output Result

| Field | Value |
|---|---|
| Push command | `uv run kaggle kernels push -p notebooks/11_lstm_multihorizon/h3` |
| Push result | `Kernel version 5 successfully pushed.` |
| Remote URL | `https://www.kaggle.com/code/alexhuaracha/11-lstm-multihorizon-h3` |
| Remote identifier | `alexhuaracha/11-lstm-multihorizon-h3` |
| Remote version | `5` |
| Terminal status | `KernelWorkerStatus.ERROR` |
| Log command | `uv run kaggle kernels logs alexhuaracha/11-lstm-multihorizon-h3 --follow --interval 10` |

The remote job reached E2 data preparation and printed the expected split counts and E2 p99 threshold (`28.4679 min`). It then failed before E2 training or residual export. Although the submitted metadata requests `GPU_T4X2`, the remote log reports `Tesla P100-PCIE-16GB` (`sm_60`), while the installed PyTorch supports `sm_70 sm_75 sm_80 sm_86 sm_90 sm_100 sm_120`.

### Exact Fatal Remote Log

```text
AcceleratorError: CUDA error: no kernel image is available for execution on the device
Search for `cudaErrorNoKernelImageForDevice` in https://docs.nvidia.com/cuda/cuda-runtime-api/group__cudart__types.html for more information.
```

The exception occurred at `model.to(torch.device(device))` during E2 LSTM training. Kaggle reported the terminal kernel state as `KernelWorkerStatus.ERROR`.

## Output Inventory

No output download was attempted because the terminal status is `ERROR`, not success. Therefore no timestamped diagnostic output directory, output filename inventory, checksums, or keyed residual export exists for this submission.

## Scientific Result Status

**Blocked — no validated scientific result.** This version established only that the submitted regenerated notebook uses the expected E2 split counts and full-frame p99 ceiling before failing on incompatible remote GPU hardware. It did not train E2, produce residuals, or satisfy the Phase 5 alignment gate. The legacy residual CSV remains untouched, and no local builder/gate/result artifact was changed or run.
